module.exports = {
    loginDoctor(req, res, next) {
        let docterCredentials = req.body.credentials;

        //TODO: login
    },

    logoutDoctor(req, res, next) {
        //TODO: logout
    }
}
